name: Daily Institution News Scraper

on:
  schedule:
    - cron: '0 6 * * *'   # Batch 1 — 01:00 ECT (INIAP, MAG, AGROCALIDAD, INAMHI)
    - cron: '0 8 * * *'   # Batch 2 — 03:00 ECT (ProEcuador, BCE, MIPRO)
    - cron: '0 10 * * *'  # Batch 3 — 05:00 ECT (SRI, ARCSA, SENESCYT, SERCOP)
    - cron: '0 12 * * *'  # Batch 4 — 07:00 ECT (Min. Interior, Min. Gobierno, Presidencia, ECU911)
    - cron: '0 14 * * *'  # Batch 5 — 09:00 ECT (CNE, Asamblea, Corte Const.)
    - cron: '0 16 * * *'  # Batch 6 — 11:00 ECT (PGE, Contraloria, Reg. Oficial)
  workflow_dispatch:
    inputs:
      batch:
        description: 'Batch (1=INIAP/MAG/AGRO/INAMHI, 2=ProEC/BCE/MIPRO, 3=SRI/ARCSA/SEN/SER, 4=Interior/Gob/Pres/ECU911, 5=CNE/Asam/Corte, 6=PGE/Cont/RegOf, 0=all)'
        required: false
        default: '0'

jobs:
  scrape:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: |
          cd scripts
          npm install

      - name: Determine batch
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "BATCH=${{ github.event.inputs.batch }}" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 6 * * *" ]; then
            echo "BATCH=1" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 8 * * *" ]; then
            echo "BATCH=2" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 10 * * *" ]; then
            echo "BATCH=3" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 12 * * *" ]; then
            echo "BATCH=4" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 14 * * *" ]; then
            echo "BATCH=5" >> $GITHUB_ENV
          elif [ "${{ github.event.schedule }}" = "0 16 * * *" ]; then
            echo "BATCH=6" >> $GITHUB_ENV
          else
            echo "BATCH=0" >> $GITHUB_ENV
          fi

      - name: Run scraper
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ecuador_monitor }}
          BATCH: ${{ env.BATCH }}
        run: |
          cd scripts
          node scraper.js

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data.json public/archive/
          git diff --staged --quiet || git commit -m "chore: update news batch ${{ env.BATCH }} [$(date -u '+%Y-%m-%d %H:%M UTC')]"
          git push
